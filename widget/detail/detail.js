var $ = require('jquery');
var app = module.exports = function(opt) {
	var detail = {
		init : function(){
			this.addEvent();
		},
		addEvent : function(){
			// if(loginInfo){
			// 	console.log(loginInfo);
			// }
		},
	};
	detail.init();
};